ktor:
  environment: local
  application:
    modules:
      - no.nav.tsm.mottak.ApplicationKt.module
  deployment:
    port: 8080
  database:
    dbUrl: $NAIS_SOMETHING_DATABASE
    dbUser: $NAIS_DB_USER_INJECTED_FOR_US
    dbPassword: $NAIS_DB_USER_INJECTED_FOR_US
