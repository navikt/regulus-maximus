ktor:
  environment: local
  development: true
  application:
    modules:
      - no.nav.tsm.mottak.ApplicationKt.module
  deployment:
    port: 8080
  database:
    dbUrl: "postgresql://localhost:5432/postgres"
    dbUser: postgres
    dbPassword: postgres
